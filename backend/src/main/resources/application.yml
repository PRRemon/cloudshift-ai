spring:
  application:
    name: cloudshift-api
#  autoconfigure:
#    exclude: org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration
#  datasource:
#    url: jdbc:postgresql://cloudshift-db.cdi088m40cgs.ap-south-1.rds.amazonaws.com:5432/postgres
    username: postgres
    password: postgresraval
    driver-class-name: org.postgresql.Driver

  datasource:
    url: jdbc:postgresql://localhost:5432/cloudshift
    username: cloudshift
    password: cloudshift
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration

security:
  jwt:
    secret: jOyDxeR4G9OXVsgucG2xSFIzZoMlFZYH5tsslbqjuigRVTFMp1JXm8ReWP9LPdmQJVoVhzb3pjiq0KGMJLmFkg==
    expiration: 86400000
springdoc:
  swagger-ui:
    path: /swagger-ui.html

  api-docs:
    path: /v3/api-docs
    enabled: true
management:
  endpoints:
    web:
      exposure:
        include: health,info

  endpoint:
    health:
      show-details: always

info:
  app:
    name: CloudShift AI
    version: 0.0.1